version: '3'

tasks:
  test:
    cmds:
      - pytest test
  linter:
    cmds:
      - flake8 . --ignore=E501 --count --max-line-length=120
  run:
    cmds:
      - python -m LiSH
  commit:
    cmds:
      - git add *
      - git status
      - git commit -m "Some fixes"
      - git push
